# ollma-webui-devenv

## Usage

```bash
# Start Container
#   PORT 11434 : ollama
#   PORT 3000 : open-webui
make start

# Download model
make pull MODEL=<model_name>

# Remove model
make rm MODEL=<model_name>
```
